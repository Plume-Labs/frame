apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: cluster-control
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/your-org/cluster-gitops
  ref:
    branch: main
  secretRef:
    name: github-token
