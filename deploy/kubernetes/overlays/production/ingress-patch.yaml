apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cluster-control-ingress
  namespace: cluster-control
spec:
  tls:
    - hosts:
        - cluster.production.example.com
      secretName: cluster-control-prod-tls
  rules:
    - host: cluster.production.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cluster-control-ui
                port:
                  name: http
