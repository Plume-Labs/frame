apiVersion: apps/v1
kind: Deployment
metadata:
  name: cluster-control-ui
  namespace: cluster-control
spec:
  template:
    spec:
      containers:
        - name: ui
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
