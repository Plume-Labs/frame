apiVersion: cilium.io/v1alpha1
kind: TracingPolicy
metadata:
  name: monitor-network-egress
spec:
  kprobes:
    - call: "tcp_connect"
      syscall: false
      args:
        - index: 0
          type: "sock"
