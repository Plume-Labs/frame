{{- if .Values.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "frame.name" . }}-controller-manager-metrics-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "frame.chartLabels" . | nindent 4 }}
    control-plane: controller-manager
spec:
  ports:
  - name: {{ include "frame.metricsPortName" . }}
    port: {{ include "frame.metricsPort" . }}
    protocol: TCP
    targetPort: {{ include "frame.metricsPort" . }}
  selector:
    {{- include "frame.selectorLabels" . | nindent 4 }}
{{- end }}
