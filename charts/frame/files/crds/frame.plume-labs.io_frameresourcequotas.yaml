---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: frameresourcequotas.frame.plume-labs.io
spec:
  group: frame.plume-labs.io
  names:
    kind: FrameResourceQuota
    listKind: FrameResourceQuotaList
    plural: frameresourcequotas
    singular: frameresourcequota
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.serviceClass
      name: ServiceClass
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    deprecated: true
    deprecationWarning: frame.plume-labs.io/v1alpha1 FrameResourceQuota is deprecated;
      use frame.plume-labs.io/v1beta1.
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FrameResourceQuota is the Schema for the frameresourcequotas
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of FrameResourceQuota
            properties:
              maxCPU:
                anyOf:
                - type: integer
                - type: string
                description: MaxCPU is the maximum total CPU allocatable.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              maxGPUs:
                description: MaxGPUs is the maximum number of GPUs allocatable across
                  all jobs in this service class.
                format: int32
                minimum: 0
                type: integer
              maxJobs:
                description: |-
                  MaxJobs is the maximum number of FrameJob objects that may exist in a
                  namespace of this service class. It is projected as the object-count quota
                  count/framejobs.frame.plume-labs.io, which the apiserver enforces on
                  creation. Completed FrameJobs keep counting until they are deleted.
                format: int32
                minimum: 0
                type: integer
              maxMemory:
                anyOf:
                - type: integer
                - type: string
                description: MaxMemory is the maximum total memory allocatable.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              serviceClass:
                description: ServiceClass this quota applies to.
                enum:
                - HIGH
                - MEDIUM
                - LOW
                type: string
            required:
            - serviceClass
            type: object
            x-kubernetes-validations:
            - message: at least one of maxGPUs, maxCPU, maxMemory, or maxJobs must
                be set
              rule: (has(self.maxGPUs) && self.maxGPUs > 0) || has(self.maxCPU) ||
                has(self.maxMemory) || (has(self.maxJobs) && self.maxJobs > 0)
          status:
            description: status defines the observed state of FrameResourceQuota
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the FrameResourceQuota resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              namespaces:
                description: |-
                  Namespaces is how many namespaces this quota currently projects into.
                  It is what makes Used interpretable: a zero Used with zero namespaces
                  means "nothing selected this quota", which is a different problem from
                  "selected, and idle". Unlike Used, this is a count rather than a set,
                  so 0 is a real measurement and is serialized rather than omitted:
                  absent means "not yet reconciled", 0 means "reconciled, matched
                  nothing".
                format: int32
                type: integer
              observedGeneration:
                description: |-
                  ObservedGeneration is the metadata.generation this status was computed
                  from. A client can compare it to metadata.generation to tell whether
                  the controller has seen the current spec yet, without knowing anything
                  about this kind's condition vocabulary.
                format: int64
                type: integer
              used:
                additionalProperties:
                  anyOf:
                  - type: integer
                  - type: string
                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                  x-kubernetes-int-or-string: true
                description: |-
                  Used is the sum of status.used across every corev1.ResourceQuota this
                  object projects into. The keys are the ones buildResourceList writes:
                  limits.cpu, limits.memory, requests.nvidia.com/gpu and
                  count/framejobs.frame.plume-labs.io. Absent until at least one
                  projected quota reports usage.
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .spec.serviceClass
      name: ServiceClass
      type: string
    - jsonPath: .status.namespaces
      name: Namespaces
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: FrameResourceQuota is the Schema for the frameresourcequotas
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of FrameResourceQuota
            properties:
              maxCPU:
                anyOf:
                - type: integer
                - type: string
                description: MaxCPU is the maximum total CPU allocatable.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              maxGPUs:
                description: |-
                  MaxGPUs is the maximum number of GPUs allocatable across all jobs in
                  this service class. Capped for the same reason as FrameJob.gpuCount
                  (T5): three orders of magnitude above the one physical card this
                  cluster has, so it constrains nothing real and turns a typo into a
                  validation error. All three stored quotas hold 0.
                format: int32
                maximum: 1024
                minimum: 0
                type: integer
              maxJobs:
                description: |-
                  MaxJobs is the maximum number of FrameJob objects that may exist in a
                  namespace of this service class. Projected as the object-count quota
                  count/framejobs.frame.plume-labs.io, which the apiserver enforces on
                  creation. Completed FrameJobs keep counting until they are deleted.
                format: int32
                minimum: 0
                type: integer
              maxMemory:
                anyOf:
                - type: integer
                - type: string
                description: MaxMemory is the maximum total memory allocatable.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              serviceClass:
                description: |-
                  ServiceClass this quota applies to. Required: a quota that does not say
                  what it caps is meaningless, which is why this one member of the shared
                  enum is not optional and not defaulted. The enum lives on the
                  ServiceClass type, not here.
                enum:
                - HIGH
                - MEDIUM
                - LOW
                type: string
            required:
            - serviceClass
            type: object
            x-kubernetes-validations:
            - message: at least one of maxGPUs, maxCPU, maxMemory, or maxJobs must
                be set
              rule: (has(self.maxGPUs) && self.maxGPUs > 0) || has(self.maxCPU) ||
                has(self.maxMemory) || (has(self.maxJobs) && self.maxJobs > 0)
          status:
            description: status defines the observed state of FrameResourceQuota
            properties:
              conditions:
                description: |-
                  Conditions represent the current state of the FrameResourceQuota
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              namespaces:
                description: |-
                  Namespaces is how many namespaces this quota currently projects into.
                  It is what makes Used interpretable: a zero Used with zero namespaces
                  means "nothing selected this quota", which is a different problem from
                  "selected, and idle". Unlike Used, this is a count rather than a set,
                  so 0 is a real measurement and is serialized rather than omitted:
                  absent means "not yet reconciled", 0 means "reconciled, matched
                  nothing". All three stored quotas currently match zero namespaces, so
                  the asymmetry is load-bearing on every object that exists.
                format: int32
                type: integer
              observedGeneration:
                description: |-
                  ObservedGeneration is the metadata.generation this status was computed
                  from. A client can compare it to metadata.generation to tell whether
                  the controller has seen the current spec yet, without knowing anything
                  about this kind's condition vocabulary.
                format: int64
                type: integer
              used:
                additionalProperties:
                  anyOf:
                  - type: integer
                  - type: string
                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                  x-kubernetes-int-or-string: true
                description: |-
                  Used is the sum of status.used across every corev1.ResourceQuota this
                  object projects into. The keys are the ones buildResourceList writes:
                  limits.cpu, limits.memory, requests.nvidia.com/gpu and
                  count/framejobs.frame.plume-labs.io. A key no namespace reported is
                  absent, not zero — this is a set, so omitempty is right here and
                  deliberately wrong on Namespaces below.
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
